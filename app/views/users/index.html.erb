<h1>ユーザ一覧</h1>

<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.email, user %></li>
  <% end %>
</ul>

<%= paginate @users %>

<%= link_to t('devise.shared.links.back'), books_path, class: "navbar-link" %>
