<p style="color: green"><%= notice %></p>

<h1><%= t '.title' %></h1>

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to t("show_this_book"), book %>
    </p>
  <% end %>
</div>

<%= link_to t("new_book"), new_book_path %>
